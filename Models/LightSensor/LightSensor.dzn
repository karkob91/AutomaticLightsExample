interface ILightSensor {
    out void LowLight();
    out void HighLight();
    enum State {Low, High};

    behavior {
        State state = State.Low;
        [state.High] {
            on inevitable: {
                state = State.Low;
                LowLight;
            }
            on inevitable: {
                HighLight;
            }
        }
        [state.Low] {
            on inevitable: {
                state = State.High;
                HighLight;
            }
            on inevitable: {
                LowLight;
            }
        }
    }
}